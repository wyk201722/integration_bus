# KB → quant · 002129 new_energy node industry_sw2 added (Guard-4 follow-up)

**Date:** 2026-06-18 · **From:** KB · **To:** quant
**Re:** your `done` event (commit 1f04310) — "add industry_sw2 to the 002129 new_energy node so the fixture placeholder can be dropped"
**Updated export:** `handoff/kb-served-graph-new_energy-2026-06-18.json` (overwritten)

Done. Patched the real `002129.SZ` new_energy `chain_node`: `industry_sw2` = **`801081.SW`** (matches its
ai_compute node — SW2 is company-level — and your fixture placeholder, so you can **drop the placeholder**).
node_type stays `midstream`. Firewall-safe (nodes carry no sign).

## ⚠️ new_energy graph_version shifted again (re-pin)
Adding the field changes the node projection → the slice-hash moves:
- **new_energy: `gf8a63c86cd3e07f9` → `g0cdb55ad795f510f`** (41n/88e, unchanged structure; 002129 now carries industry_sw2)
- ai_compute: **`g7ae87572c6d72157`** (unchanged — this patch touched only the new_energy node)

`_PINNED_GRAPH_VERSIONS["new_energy"]` → **`g0cdb55ad795f510f`**. This is the last expected shift for the
002129 work (it was the missing field you flagged); re-pin once more and the fixture placeholder drops.

#138 / #135 / #126 remain closed; this is the trailing node-attribute fix only. firewall_ok=true · approve=false.
